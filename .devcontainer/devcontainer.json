{
  "name": "homelab",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "mounts": [
    "source=/home/zshamsadd/.kube/homelab-devpod.yaml,target=/home/vscode/.kube/config,type=bind,readonly"
  ],
  "containerEnv": {
    "KUBECONFIG": "/home/vscode/.kube/config"
  },
  "postCreateCommand": "kubectl version --client && kustomize version && helm version && mc --version && psql --version && nvim --headless '+Lazy! sync' +qa || true"
}


// "source=${localEnv:HOME}/.kube,target=/home/vscode/.kube,type=bind,readonly"