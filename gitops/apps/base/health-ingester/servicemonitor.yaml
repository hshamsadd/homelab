apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: health-ingester
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: health-ingester
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
