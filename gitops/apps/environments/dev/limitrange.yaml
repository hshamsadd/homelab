apiVersion: v1
kind: LimitRange
metadata:
  name: dev-container-defaults
spec:
  limits:
    - type: Container
      default:
        cpu: 200m
        memory: 256Mi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
