apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: wallabag-credentials-vault
  namespace: dev
spec:
  type: kv-v2
  mount: kv
  path: kubernetes/dev/wallabag
  vaultAuthRef: dev-vault-auth
  refreshAfter: 1m

  destination:
    create: true
    overwrite: true
    name: wallabag-credentials-vault
    transformation:
      excludeRaw: true

  rolloutRestartTargets:
    - kind: Deployment
      name: wallabag
