apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: minio-dev-local-storage
  volumeName: minio-dev-local-pv
  resources:
    requests:
      storage: 5Gi
