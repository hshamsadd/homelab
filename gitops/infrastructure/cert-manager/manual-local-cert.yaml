apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-cluster-cert
  namespace: infrastructure
spec:
  secretName: local-cluster-cert
  dnsNames:
    - "*.home"
  issuerRef:
    name: local-selfsigned-issuer
    kind: ClusterIssuer
